# Terraform
**/terraform.tfvars
**/.terraform/
**/*.tfstate
**/*.tfstate.*
**/.terraform.lock.hcl
*.tfplan
crash.log
crash.*.log

# Kubeconfig (often contains secrets)
kubeconfig
*.kubeconfig

# Env / local
.env
.env.*
!.env.example

ansible/inventory/*/group_vars/all/vault

# Vault unseal keys (SENSITIVE)
.vault-keys.json
.[AI_ASSISTANT]/
[AI_ASSISTANT].md
.[AI_ASSISTANT]/settings.local.json
