- name: Install Longhorn
  gather_facts: true
  hosts: localhost
  tasks:

    - include_role:
        name: longhorn
      tags:
        - longhorn
        - management-apps
        - apps
        - install
