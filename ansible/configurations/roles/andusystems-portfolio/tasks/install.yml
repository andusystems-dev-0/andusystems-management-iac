- name: install | Install Portfolio Cluster Apps
  kubernetes.core.k8s:
    state: present
    definition: "{{ lookup('template', repo_root + '/apps/andusystems-portfolio/manifest.yml') | from_yaml_all | list }}"
  environment:
    KUBECONFIG: "{{ kubeconfig }}"
