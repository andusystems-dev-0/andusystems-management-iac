- name: install | Apply Cluster Status ArgoCD Application
  kubernetes.core.k8s:
    state: present
    definition: "{{ lookup('template', repo_root + '/apps/cluster-status/argocd-app.yml') | from_yaml_all | list }}"
  environment:
    KUBECONFIG: "{{ kubeconfig }}"
